pvc:
  accessMode: ReadWriteOnce
  size: 1Gi
  storageClass: ""

postgresql:
  fullnameOverride: notes-app-postgresql
  primary:
    podLabels:
      app: notes-app-postgresql
    persistence:
      existingClaim: notes-app-postgresql
  auth:
    enablePostgresUser: false
    username: u_notes
    database: notes
    existingSecret: pg-user-password
    secretKeys:
      userPasswordKey: user-password
backend:
  deployment:
    image:
      repository: gcr.io/containerisation/containerization-backend
    # repository: containerization-backend
      tag: latest
      pullPolicy: Always
    env:
      production: true
      dbUser: u_notes
      dbDriver: postgresql+aiopg
      dbHost: notes-app-postgresql
      dbPort: 5432
      singleNode: true
    ports:
      containerPort: 8000


